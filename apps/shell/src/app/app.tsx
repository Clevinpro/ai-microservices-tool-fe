/**
 * Shell routing is defined in `src/router.tsx` (TanStack Router).
 * This default export remains for tests and tooling that import `./app/app`.
 */
export function App() {
  return null;
}

export default App;
